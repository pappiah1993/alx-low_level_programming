#include <stdio.h>

/**
 * main - represent the main function of the task
 *
 * Return: is successful when 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
